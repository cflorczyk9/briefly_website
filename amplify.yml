version: 1
applications:
  - appRoot: front-end/website
    frontend:
      phases:
        preBuild:
          commands: []
        build:
          commands: []
      artifacts:
        baseDirectory: .
        files:
          - "**/*"
      cache:
        paths: []
